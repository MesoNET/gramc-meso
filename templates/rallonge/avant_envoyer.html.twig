{% extends 'default/base.html.twig' %}

{% block body %}

{% if not erreurs == null %}
    <section>
        <div class="information attention">
            <h2>Envoi de la demande pas possible actuellement</h2>
            <p>Votre demande d'extension pour le projet {{  projet }} ne <strong>peut pas</strong> être envoyée pour expertise en l'état.
            Merci de corriger les erreurs:</p>
            {% for erreur in erreurs %}
                <p><strong>ERREUR:</strong> {{ erreur }}</p>
            {% endfor %}
        </div>
        <nav><ul><li>
            <a class="lien_actif" href="{{ path( 'consulter_rallonge', { 'id': rallonge.idRallonge } )}}" title="Retourner à la rallonge">Retourner</a>
        </li></ul></nav>
    </section>

{% else %}

    <section>
        <h2>Projet {{ projet }}, version {{ rallonge.version.nbversion}}: Confirmation d'envoi d'une demande d'extension</h2>
        <div class="information">
            <h2>Attention</h2>Vous êtes sur le point d'envoyer votre demande en validation.<br>

        </div>
        <nav>
            <ul>
                <li>
                    <a class="lien_actif" href="{{ path( 'consulter_rallonge', { 'id': rallonge.idRallonge } )}}" title="Retourner à la rallonge">ANNULER</a>
                </li>
                <li>
                    <a class="lien_actif" href="{{ path( 'envoyer_rallonge', { 'id': rallonge.idRallonge } ) }}" title="Envoyer votre demande à l'expert">ENVOYER</a>
                </li>
            </ul>
        </nav>
    </section>

{% endif %}

{% endblock %}
